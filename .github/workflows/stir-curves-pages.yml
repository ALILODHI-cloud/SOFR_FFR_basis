name: STIR curves — fetch, build, deploy

on:
  push:
    branches: [main]
    paths:
      - 'analyze_stir_curves.py'
      - 'analyze_sonia.py'
      - 'build_stir_curves_dashboard.py'
      - 'requirements-sonia.txt'
      - '.github/workflows/stir-curves-pages.yml'
  schedule:
    # Weekdays: ~10:00 London + ~18:30 London (post CME/ICE close)
    - cron: "0 9 * * 1-5"
    - cron: "30 17 * * 1-5"
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: stir-curves-pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r requirements-sonia.txt
          playwright install chromium

      - name: Fetch Barchart + rebuild STIR curves dashboard
        run: |
          python analyze_stir_curves.py || echo "::warning::Barchart fetch failed; building from committed snapshot"
          python build_stir_curves_dashboard.py
          cp stir_curves_dashboard.html docs/stir_curves_dashboard.html
          cp portal.html docs/portal.html || true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: docs

      - name: Commit refreshed data (if changed)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add stir_curves_data.json stir_curves_dashboard.html docs/stir_curves_dashboard.html portal.html docs/portal.html
          git diff --staged --quiet || git commit -m "Auto-refresh STIR curves $(date -u +%Y-%m-%dT%H:%MZ)"
          git push || true

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
