name: SONIA dashboard — fetch, build, deploy

on:
  push:
    branches: [main]
    paths:
      - 'analyze_sonia.py'
      - 'build_sonia_dashboard.py'
      - 'requirements-sonia.txt'
      - '.github/workflows/sonia-pages.yml'
  schedule:
    # Weekdays: ~10:00 London + ~18:30 London (post ICE close)
    - cron: "0 9 * * 1-5"
    - cron: "30 17 * * 1-5"
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: sonia-pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r requirements-sonia.txt
          playwright install chromium

      - name: Fetch Barchart + rebuild dashboard
        run: |
          python analyze_sonia.py || echo "::warning::Barchart fetch failed; building from committed snapshot"
          python build_sonia_dashboard.py
          cp sonia_dashboard.html docs/index.html
          cp sonia_dashboard.html docs/sonia_dashboard.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: docs

      - name: Commit refreshed data (if changed)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add sonia_dashboard_data.json sonia_dashboard.html docs/
          git diff --staged --quiet || git commit -m "Auto-refresh SONIA dashboard $(date -u +%Y-%m-%dT%H:%MZ)"
          git push || true

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
